name: Deploy to Server

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Laravel App
    runs-on: ubuntu-latest
    
    steps:
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_SECRET }}

      - name: Add server to known hosts
        run: |
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy ke server
        run: |
          ssh ${{ secrets.SSH_USERNAME }}@${{ secrets.SERVER_HOST }} << 'ENDSSH'
            echo "🚀 ========================================="
            echo "🚀 Mulai deployment nih..."
            echo "🚀 ========================================="
            echo ""
            
            # Masuk ke direktori project
            echo "📁 Masuk ke direktori project..."
            cd /path/to/your/project || exit 1
            echo "✅ Sudah di direktori project"
            echo ""
            
            # Pull latest code (kalau pakai git)
            # echo "📥 Pull latest code dari repository..."
            # git pull origin master || exit 1
            # echo "✅ Code terbaru sudah di-pull"
            # echo ""
            
            # Install/update dependencies
            # echo "📦 Install/update dependencies..."
            # composer install --no-dev --optimize-autoloader || exit 1
            # echo "✅ Dependencies sudah terinstall"
            # echo ""
            
            # Run artisan commands
            echo "🔄 ========================================="
            echo "🔄 Running Artisan Commands..."
            echo "🔄 ========================================="
            echo ""
            
            echo "📊 Running migrations..."
            php artisan migrate --force || exit 1
            echo "✅ Migrations selesai!"
            echo ""
            
            echo "⚡ Optimizing application..."
            php artisan optimize || exit 1
            echo "✅ Application sudah di-optimize!"
            echo ""
            
            echo "🧹 Clearing cache..."
            php artisan cache:clear || exit 1
            echo "✅ Cache sudah dibersihkan!"
            echo ""
            
            echo "🔧 Clearing config cache..."
            php artisan config:clear || exit 1
            echo "✅ Config cache sudah dibersihkan!"
            echo ""
            
            echo "🎉 ========================================="
            echo "🎉 Deployment selesai! Semua lancar! 🎉"
            echo "🎉 ========================================="
          ENDSSH

